{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: invoke registered Tauri commands and listen for events. No shell, no fs, no http surface granted to the frontend; least privilege until each capability is needed.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
