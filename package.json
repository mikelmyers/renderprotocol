{
  "name": "renderprotocol",
  "version": "0.0.0",
  "private": true,
  "description": "v0 of the render surface — an agent-native operator interface on MCP + MCP Apps",
  "scripts": {
    "dev": "bash scripts/dev.sh",
    "dev:mock": "pnpm --filter @renderprotocol/mock-mcp-server dev",
    "dev:host": "pnpm --filter @renderprotocol/host tauri dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.12.0"
}
